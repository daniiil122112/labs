#include <stdio.h>

main()
{
	float v1,v2,t;

	printf("speed v1 is:");
	scanf("%f",&v1);

	printf("\n speed v2 is:");
	scanf("%f",&v2);

	printf("\n time is:");
	scanf("%f",&t);

	printf("\n distance is: %f\n",((v1+v2)*t));
	system("pause");
	return 0;
}

